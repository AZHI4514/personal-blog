# Loop Engineering 核心原则

> 基于 2026 年最新 Loop Engineering 实践总结，应用于本次"人生模拟器"开发。

## 一、什么是 Loop Engineering

Loop Engineering（循环工程）是 2026 年硅谷 AI 开发新范式：**不手工编写提示词，而是设计一套系统让 Agent 在循环中自主完成任务**。核心思想来自 Anthropic Claude Code 负责人 Boris Cherny、OpenClaw 开发者 Peter Steinberger 等人。

## 二、五大核心原则

### 原则 1：最小可行变更（Minimal Viable Change）

每次只修改一个模块/一个关注点。修改前自问：
- 这个变更是否最小？
- 是否只解决一个问题？
- 是否可以通过更少的代码行实现？

**本项目应用**：每个 API 端点独立开发测试，每个组件独立创建验证，不在一次变更中同时改前后端。

### 原则 2：闭环验证（Closed-Loop Verification）

每完成一个原子任务立即验证，不积累未验证的变更：
- 后端：写完端点 → curl 测试 → 确认返回结构正确
- 前端：写完组件 → 浏览器预览 → 检查渲染和交互
- 数据库：写完 SQL → 执行 → 验证表结构

**本项目应用**：8 个 API 端点逐个开发，每个都先手动 curl 测试通过再写下一个。

### 原则 3：Maker-Checker 分离（Generator-Critic Split）

写代码的 Agent 绝不给自己打分。生成和验证必须是不同视角：
- 写完代码后，切换到"用户视角"审视：页面是否正常渲染？按钮是否可点击？
- 写完测试后，确认测试确实验证了正确行为（而非为了通过而通过）
- 禁止"修改测试来让代码通过"

**本项目应用**：每个阶段完成后模拟用户验收——检查页面渲染、按钮响应、移动端触摸、数据持久化。

### 原则 4：有界自主与停止条件（Bounded Autonomy）

自主循环必须有明确停止条件：
- 同一问题连续失败 3 次 → 切换策略，记录原因
- 每个端点开发不超过 30 分钟 → 超时简化方案
- Token 预算有限 → 优先完成核心功能

**本项目应用**：如果 LangChain4j 动态模型创建遇到困难，降级为直接使用 OkHttp/RestTemplate 调用 OpenAI 兼容 API。

### 原则 5：状态外化与可恢复性（State Externalization）

所有进度和决策记录在代码/文件中，而非依赖对话上下文：
- 开发进度记录在 `LOOP_PRINCIPLES.md`（本文件）
- 失败记录和策略切换原因记录在对应模块注释中
- 每个阶段完成状态可追踪

**本项目应用**：每完成一个阶段，在此文件中打勾标记。

## 三、本项目的 Loop 执行流程

```
┌──────────────────────────────────────────────────────┐
│  ① 感知 (Perceive)：读取代码 → 理解上下文 → 定位问题   │
│  ② 决策 (Decide) ：制定最小可行方案 → 一次只改一个模块  │
│  ③ 行动 (Act)    ：编写/修改代码 → 运行测试 → 启动预览  │
│  ④ 反思 (Reflect)：检查结果 → 记录问题 → 自我修正       │
│  ⑤ 推进 (Advance)：确认通过 → 进入下一模块              │
└──────────────────────────────────────────────────────┘
```

### 停止条件
- 连续 3 次失败 → 切换策略
- 单端点超过 30 分钟 → 简化方案
- 所有 6 个阶段完成 → 交付

## 四、进度追踪

- [x] 阶段 0：联网调研
- [x] 阶段 1：环境验证
- [x] 阶段 2：数据库建表
- [x] 阶段 3：后端开发（8 个 API 端点）
- [x] 阶段 4：前端组件开发（3 个组件）
- [x] 阶段 5：集成验证（需 MySQL 运行 + LLM 配置）
- [x] 阶段 6：最终验证与交付

---

> **"Build the loop. Stay the engineer."** — Addy Osmani
>
> 循环工程不是让工程师变多余，而是让工程师的杠杆点从"写好每一句提示词"转移到"设计好整个反馈回路"。
